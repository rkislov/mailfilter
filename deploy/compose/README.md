# Compose Deployment Notes

This directory is reserved for environment-specific compose overlays and production deployment snippets.

Suggested follow-up files:

- `docker-compose.override.yml` for local developer changes
- `docker-compose.prod.yml` for hardened production settings
- secrets mounting examples for commercial AV adapters and external AI providers
